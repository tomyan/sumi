<style>
.container {
    border: single;
    padding: 1 2;
}
.title {
    color: green;
    font-weight: bold;
}
.hint {
    color: cyan;
    opacity: dim;
}
.modal {
    border: single;
    border-color: yellow;
    padding: 1 2;
    background: black;
}
.modal-title {
    color: yellow;
    font-weight: bold;
}
</style>

<script>
showModal := sumi.New(false)

func handleKey(evt sumi.Event) {
    if evt.Kind == sumi.EventSignal { sumi.Quit(); return }
    if evt.Rune == 'q' || (evt.Ctrl && evt.Rune == 'c') { sumi.Quit(); return }
    if evt.Kind == sumi.EventKey {
        showModal.Set(!showModal.Get())
    }
}
</script>

<div class="container" onkey="handleKey">
    <span class="title">Modal Demo</span>
    <span class="hint">Press any key to toggle modal, q to quit</span>
    <span>Background content here</span>
</div>
{if showModal.Get()}
    <div class="modal" position="fixed" top="5" left="10" width="40" height="8" z-index="2">
        <span class="modal-title">Modal Dialog</span>
        <span>This is a fixed-position modal overlay.</span>
        <span class="hint">Press any key to close</span>
    </div>
{/if}
